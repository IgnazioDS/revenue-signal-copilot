# Revenue Signal Copilot requires no environment variables to build and run.
# This dashboard is a static Next.js frontend that fetches from public API endpoints.
#
# Future environment variables should be prefixed with NEXT_PUBLIC_ to be accessible
# in the browser, or kept private (without prefix) for server-side use only.
